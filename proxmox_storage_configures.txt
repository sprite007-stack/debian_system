✅ PART 1 — Verify sdb is unused

Run:

lsblk -f


You should see no filesystem, no mountpoint, no partitions under sdb.

Example expected:

sdb      8:16   0  74.5T  0 disk 


If it already has partitions, I’ll tell you how to wipe safely.

✅ PART 2 — Wipe disk (only if empty)

If sdb has old signatures:

wipefs -a /dev/sdb

✅ PART 3 — Create a GPT partition table
parted /dev/sdb --script mklabel gpt

✅ PART 4 — Create a single large partition
parted -a optimal /dev/sdb --script mkpart primary ext4 0% 100%


Now check:

lsblk


You should see:

sdb
└─sdb1   8:17  0  74.5T  0 part

✅ PART 5 — Format the partition (choose ext4 or xfs)
For large continuous video files, XFS is better

Fast metadata, faster I/O for huge files.

mkfs.xfs -f /dev/sdb1


If you prefer ext4:

mkfs.ext4 /dev/sdb1

✅ PART 6 — Create mount point

Since you want this disk to be your media storage:

mkdir -p /mnt/media

✅ PART 7 — Mount the drive
mount /dev/sdb1 /mnt/media


Check:

df -h /mnt/media


You should now see:

/dev/sdb1    74T    0T   74T   0%   /mnt/media

✅ PART 8 — Make the mount permanent

Edit fstab:

nano /etc/fstab


Add:

For XFS:
/dev/sdb1   /mnt/media   xfs   defaults,noatime   0  0

For EXT4:
/dev/sdb1   /mnt/media   ext4   defaults,noatime   0  0


Test:

mount -a


No errors → good.

🔥 PART 9 — Add to Proxmox storage GUI

Since this is file storage, add it as a Directory:

Datacenter → Storage → Add → Directory

Field	Value
ID	media-sdb
Directory	/mnt/media
Content	VZDump, ISO, Snippets, or just select “Other”
Nodes	your node
Shared	No (unless NFS/SMB exported later)

Now Proxmox knows about the storage.

🔥 PART 10 — Export to Linux Mint or Windows (optional)

You said earlier you want to access the large storage from your Mint desktop.

If you want NFS:

Add to /etc/exports on Proxmox:

/mnt/media 192.168.1.0/24(rw,sync,no_subtree_check,no_root_squash)


Apply:

exportfs -arv
systemctl restart nfs-kernel-server


Mount from Mint:

sudo mount -t nfs proxmox-ip:/mnt/media /mnt/proxmox-media